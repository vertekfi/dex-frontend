export default function userGetUserStakesQuery<T>(poolIds: string[]) {
  // const { account, isWalletReady } = useWeb3();

  // const queryKey = reactive(QUERY_KEYS.Account.GaugeStakes(account));

  // const queryFn = async () => {
  //   return await aequalizerService.getUserGaugeStakes(account.value, poolIds);
  // };

  // const queryOptions = reactive({
  //   refetchOnWindowFocus: false,
  //   enabled: isWalletReady.value
  // });

  return {};
}
